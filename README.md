Employee Management System
A Java-based web application designed to manage employee records efficiently using JSP, Servlets, JDBC, and MySQL.
The system supports secure session-based authentication and performs complete CRUD operations following the MVC architecture.

🚀 Features
Employee Registration & Login
Session Management using HttpSession
Role-based access control
Perform all CRUD operations (Create, Read, Update, Delete) on employee records
Clean separation of concerns using MVC architecture
Secure and efficient database interaction via JDBC

🛠 Tech Stack
Backend: Java (Servlets)
Database: MySQL
Frontend: HTML, CSS, JSP
Connectivity: JDBC
Server: Apache Tomcat
Architecture: MVC (Model–View–Controller)

⚙️ How It Works
User registers and logs in through HTML pages
Authentication is handled using HttpSession
Requests are processed by Servlets (Controller layer)
Data is fetched or updated using JDBC
JSP pages render responses dynamically
